<section class="empty-state card" aria-label="No notes selected">
  <div class="inner">
    <h2>Welcome</h2>
    <p>Select a note from the left or create a new one.</p>
  </div>
</section>

<style>
  .empty-state {
    height: 100%;
    display: grid;
    place-items: center;
    padding: 20px;
  }
  .inner {
    text-align: center;
    max-width: 520px;
    color: var(--muted);
  }
  h2 {
    margin: 0 0 8px;
    color: var(--text);
  }
</style>
